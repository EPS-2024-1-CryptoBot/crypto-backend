name: Deploy Backend

on:
  workflow_dispatch:

jobs:
  build:
    name: Build and Push
    runs-on: ubuntu-22.04
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: |
          echo $DATABASE_URL
